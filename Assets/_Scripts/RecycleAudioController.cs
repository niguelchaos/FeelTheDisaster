using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RecycleAudioController : MonoBehaviour
{
    public AudioClip stopRobotClip;
    public AudioClip tetraPakClip;
    public AudioClip playerHitClip;
    public AudioClip whackRobotClip;

    public AudioSource corridorSpeaker;
    public AudioSource soundEffectsSpeaker;
    public AudioSource arenaSpeaker;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
